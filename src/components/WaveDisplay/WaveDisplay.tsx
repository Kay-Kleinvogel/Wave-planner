import { Card, CardContent, Typography } from "@mui/material";
import React from "react";

const WaveDisplay = () => {
  return (
    <Card>
      <CardContent>
        <Typography>WaveDisplay</Typography>
      </CardContent>
    </Card>
  );
};

export default WaveDisplay;
